services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  dashboard:
    build: ./dashboard
    ports:
      - "3000:80"
    depends_on:
      - backend
